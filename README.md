# mat-drum
MatDrum is a library focused on transform Arduino microcontroller into a simple drum module. With that you can modify different reading signal parameters, such as pad sensitivity, threshold, scan time and mask time.
